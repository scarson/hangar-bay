---
Phase: 07 - Backend F003 Detailed Ship Contract View
TaskID: 07.1
ExecutionSequence: 1
PreviousTask: null
NextTask: null
ParentPlan: /plans/implementation/00-mvp-implementation-plan-overview.md
LastUpdated: 2025-07-02
---

# Task 07.1: Enhanced Detailed Contract API with Ship Attributes & ESI Data

## 1. Objective

To enhance the existing `/contracts/{contract_id}` API endpoint to provide comprehensive detailed contract information as specified in F003, including ship attributes from ESI data, EVE image server URLs, and enhanced contract metadata. This builds upon the existing basic contract retrieval functionality to deliver a production-ready detailed view that supports the frontend's requirements for displaying comprehensive contract and ship information.

## 2. Relevant Specifications

*   `design/features/F003-Detailed-Ship-Contract-View.md`
*   `design/fastapi/00-fastapi-architecture-overview.md`
*   `design/fastapi/guides/02-observability-guide.md`
*   `design/fastapi/guides/09-testing-strategies.md`
*   `design/fastapi/patterns/02-service-construction.md`
*   `design/fastapi/patterns/06-schema-model-parity.md`
*   `design/specifications/observability-spec.md`
*   `design/specifications/security-spec.md`

## 3. Key Implementation Steps

### 3.1. Database Schema Enhancements

*   [x] **Step 1.1: Create EsiTypeCache Model**
    *   Create `EsiTypeCache` model in `models/common_models.py` to store comprehensive ESI type data including ship attributes, descriptions, and metadata
    *   Include fields: `type_id` (primary key), `name`, `description`, `category_id`, `group_id`, `market_group_id`, `dogma_attributes` (JSON), `dogma_effects` (JSON), `mass`, `volume`, `capacity`, `icon_id`, `graphic_id`, `published`, `raw_esi_response` (JSON for future-proofing)
    *   Add appropriate indexes for performance optimization
    *   **AI Prompt:** "Create a comprehensive EsiTypeCache SQLAlchemy model following the established patterns in the codebase. Include all fields needed for ship attribute display and EVE image server URL generation. Use JSON columns for dogma_attributes and dogma_effects. Follow the existing naming conventions and include appropriate indexes."

*   [x] **Step 1.2: Create Alembic Migration**
    *   Generate Alembic migration for the new EsiTypeCache table
    *   **AI Prompt:** "Generate an Alembic migration to create the esi_type_cache table with all the required fields and indexes."

### 3.2. Enhanced Pydantic Schemas

*   [x] **Step 2.1: Create EsiTypeSchema**
    *   Create `EsiTypeSchema` in `schemas/contracts.py` with schema-model parity to `EsiTypeCache`
    *   Include computed fields for EVE image server render and icon URLs
    *   **AI Prompt:** "Create a comprehensive EsiTypeSchema Pydantic model that matches the EsiTypeCache SQLAlchemy model, following the schema-model parity pattern. Include computed fields for EVE image server URLs (render and icon) using the type_id field."

*   [x] **Step 2.2: Create DetailedContractSchema**
    *   Create `DetailedContractSchema` that extends `ContractSchema` with enhanced item information
    *   Include ship attributes, descriptions, and image URLs for each item
    *   Group items by primary ships vs. additional items as per F003 requirements
    *   **AI Prompt:** "Create a DetailedContractSchema that extends the existing ContractSchema, adding comprehensive ship attribute information, descriptions, and EVE image server URLs. Structure the response to distinguish between primary ship items and additional included items as specified in F003."

*   [x] **Step 2.3: Create ShipAttributeSchema**
    *   Create schema for displaying ship attributes in organized groups (Tank, Capacitor, Targeting, etc.)
    *   Process dogma_attributes JSON into user-friendly attribute groups
    *   **AI Prompt:** "Create a ShipAttributeSchema that organizes dogma_attributes from the EsiTypeCache into logical groups like 'Tank', 'Capacitor', 'Targeting', 'Slots', etc. Provide both 'key_attributes' and 'all_attributes' as specified in F003."

### 3.3. Enhanced Service Layer

*   [x] **Step 3.1: Create ContractDetailsService**
    *   Create `ContractDetailsService` in `services/contract_details_service.py` following the service construction pattern
    *   Implement comprehensive contract retrieval with ESI type data joins
    *   Include ship attribute processing and EVE image URL generation
    *   **AI Prompt:** "Create a ContractDetailsService class following the established service construction pattern. Implement a method to retrieve a contract with all its items and join with EsiTypeCache to provide comprehensive ship details. Include logic to process ship attributes and generate EVE image server URLs."

*   [ ] **Step 3.2: Implement Ship Attribute Processing**
    *   Create helper methods to process dogma_attributes JSON into organized attribute groups
    *   Implement logic to identify key ship attributes vs. all attributes
    *   **AI Prompt:** "Implement helper methods in ContractDetailsService to process the dogma_attributes JSON field from EsiTypeCache into organized attribute groups. Create logic to identify and extract key ship attributes (resistances, EHP, capacitor, targeting, slots) and provide comprehensive attribute categorization."

*   [x] **Step 3.3: Implement EVE Image Server URL Generation**
    *   Create utility methods to generate render and icon URLs using EVE image server
    *   Support multiple sizes and handle fallbacks for missing images
    *   **AI Prompt:** "Create utility methods to generate EVE image server URLs for ship renders and icons. Use the patterns https://images.evetech.net/types/{type_id}/render?size={size} and https://images.evetech.net/types/{type_id}/icon?size={size}. Include size options and error handling for missing images."

### 3.4. API Endpoint Enhancement

*   [x] **Step 4.1: Enhance Existing Contract Detail Endpoint**
    *   Modify the existing `/contracts/{contract_id}` endpoint to use the new ContractDetailsService
    *   Implement comprehensive error handling for missing contracts and ESI data
    *   Add structured logging for the detailed view operations
    *   **AI Prompt:** "Enhance the existing /contracts/{contract_id} endpoint in api/contracts.py to use the new ContractDetailsService. Implement comprehensive error handling, structured logging following the Key Events schema, and return the ContractDetailsSchema response."

*   [x] **Step 4.2: Add Ship Attribute Query Parameter**
    *   Add optional query parameter to control attribute detail level (key_attributes vs all_attributes)
    *   Implement conditional attribute processing based on the parameter
    *   **AI Prompt:** "Add an optional query parameter 'attribute_detail' to the contract detail endpoint to control whether to return 'key_attributes' or 'all_attributes'. Implement conditional processing in the service layer based on this parameter."

### 3.5. Testing Implementation

*   [ ] **Step 5.1: Unit Tests for ESI Type Service**
    *   Create comprehensive unit tests for ESITypeService covering all methods
    *   Test cache behavior, ESI data fetching, ship type validation, and attribute extraction
    *   Mock ESI client responses and database interactions
    *   Aim for 100% test coverage of the service layer
    *   **AI Prompt:** "Create comprehensive unit tests for ESITypeService in tests/services/test_esi_type_service.py. Test all methods including get_type_info, get_multiple_types, ship_attributes, and image URL generation. Mock database and ESI client interactions for isolated testing."

*   [ ] **Step 5.2: Unit Tests for Enhanced Contract Service** 
    *   Create unit tests for the get_contract_details function in contract_details_service.py
    *   Test contract retrieval, item enhancement, ship details processing
    *   Mock ESI Type Service interactions and verify proper data flow
    *   Test error handling for missing contracts and ESI failures
    *   **AI Prompt:** "Create unit tests for the get_contract_details function covering successful contract retrieval, ESI data enhancement, ship details processing, and error handling for missing contracts and ESI failures."

*   [ ] **Step 5.3: Integration Tests for Detailed Contract API**
    *   Create integration tests for both /contracts/{contract_id} and /contracts/details/{contract_id} endpoints
    *   Test with real database and mocked ESI responses using pytest-vcr
    *   Verify response schemas match DetailedContractSchema exactly
    *   Test error cases (404, ESI failures) and proper HTTP status codes
    *   **AI Prompt:** "Create integration tests for the detailed contract API endpoints in tests/api/test_contracts_detailed.py. Use pytest-vcr for ESI mocking and verify both happy path and error scenarios with ContractDetailsSchema validation."

*   [ ] **Step 5.4: Database Integration Tests**
    *   Test EsiTypeCache model CRUD operations and constraints
    *   Verify proper indexing and query performance
    *   Test PostgreSQL upsert behavior in ESI Type Service
    *   **AI Prompt:** "Create database integration tests for EsiTypeCache model testing CRUD operations, constraints, indexing, and upsert behavior in PostgreSQL."

*   [ ] **Step 5.5: Live ESI Integration Tests**
    *   Create live ESI tests using pytest-vcr for real API validation
    *   Test actual ship type data retrieval and caching
    *   Validate against known ship type IDs (e.g., Rifter type_id=587)
    *   Use esi_live marker for periodic execution
    *   **AI Prompt:** "Create live ESI integration tests using pytest-vcr to validate real ship type data retrieval and caching against known ship type IDs like Rifter (587)."

### 3.6. Observability Implementation

*   [ ] **Step 5.1: Add Structured Logging**
    *   Implement structured logging throughout the service layer following the Key Events schema
    *   Log contract retrieval operations, ESI data joins, and performance metrics
    *   **AI Prompt:** "Add comprehensive structured logging to the ContractDetailsService following the Key Events schema defined in the observability guide. Log contract retrieval operations, database query performance, and any ESI data processing steps."

*   [ ] **Step 5.2: Add Custom Metrics**
    *   Implement custom Prometheus metrics for detailed contract views
    *   Track performance metrics for attribute processing and ESI data joins
    *   **AI Prompt:** "Add custom Prometheus metrics for the detailed contract view functionality. Track metrics like detailed_contract_requests_total, esi_data_join_duration_seconds, and ship_attribute_processing_duration_seconds."

## 4. AI Implementation Guidance

*   Use the existing Contract and ContractItem models as the foundation; do not recreate them
*   Follow the established schema-model parity pattern rigorously
*   Implement the service construction pattern for the DetailedContractService
*   Use the existing database session management patterns from the testing guide
*   Implement comprehensive error handling for missing ESI data gracefully
*   Ensure all JSON fields (dogma_attributes, dogma_effects) are properly typed and processed
*   Generate EVE image server URLs following the documented patterns in F003
*   Follow the established structured logging patterns with the Key Events schema

## 5. Definition of Done (DoD)

*   [ ] **Implementation Verified & Complete:** All placeholder and scaffold code has been replaced with final logic. Each criterion below has been rigorously verified against a definitive source of truth (e.g., the current codebase, an executed test suite, a design specification). **Marking any DoD item as complete without direct, evidence-based verification is a process violation.**
*   [ ] **EsiTypeCache Model Created:** EsiTypeCache SQLAlchemy model is implemented with all required fields and indexes, following established patterns
*   [ ] **Alembic Migration Applied:** Database migration for EsiTypeCache table is created and can be applied successfully
*   [ ] **Enhanced Schemas Implemented:** ContractDetailsSchema, EsiTypeSchema, and ShipAttributeSchema are implemented with proper schema-model parity
*   [ ] **ContractDetailsService Implemented:** Service layer properly retrieves contracts with ESI data joins and processes ship attributes
*   [ ] **API Endpoint Enhanced:** Existing contract detail endpoint returns comprehensive detailed contract information as specified in F003
*   [ ] **EVE Image Server URLs Generated:** Ship render and icon URLs are properly generated using EVE image server patterns
*   [ ] **Ship Attributes Processed:** Dogma attributes are organized into logical groups with key_attributes and all_attributes views
*   [ ] **Comprehensive Testing:** Unit and integration tests cover all new functionality with >85% coverage
*   [ ] **Observability Implemented:** Structured logging and custom metrics are implemented following established patterns
*   [ ] **Error Handling Robust:** All error scenarios (missing contracts, missing ESI data) are handled gracefully with appropriate HTTP responses
*   [ ] All new code is committed to the `dev` branch
*   [ ] Adherence to architectural guidelines in `design/fastapi/00-fastapi-architecture-overview.md` has been verified

## 6. Challenges & Resolutions

*   (Placeholder for any challenges encountered and their resolutions during this task.)

## 7. Cross-Cutting Concerns Review

### 7.1. Security
*   [x] **Secure Design:** Contract ID path parameter validation prevents injection attacks
*   [x] **Input Validation:** Pydantic schemas provide comprehensive input validation
*   [x] **Output Encoding:** JSON responses are properly encoded by FastAPI
*   [x] **Authentication/Authorization:** Public endpoint requires no authentication as per F003 scope
*   [x] **Secrets Management:** No additional secrets required for this implementation
*   [x] **Dependency Management:** All dependencies are managed through pyproject.toml with version constraints
*   **Notes:** This is a read-only API endpoint that exposes public contract data. No authentication is required per F003 specifications. Input validation is handled by Pydantic schemas and FastAPI's automatic validation.

### 7.2. Observability
*   [ ] **Structured Logging:** Key Events schema implemented for contract retrieval and ESI data processing
*   [ ] **Key Events Logged:** Contract access, ESI data joins, ship attribute processing, and performance metrics
*   [ ] **Error Logging:** Comprehensive error logging for missing contracts and ESI data scenarios
*   [ ] **Correlation IDs:** Request ID correlation implemented via existing middleware
*   [ ] **Metrics:** Custom Prometheus metrics for detailed contract operations and performance
*   **Notes:** Comprehensive observability implementation following established patterns in the observability guide.

### 7.3. Testing
*   [ ] **Unit Tests:** Service layer methods for contract retrieval, attribute processing, and URL generation
*   [ ] **Integration Tests:** Full API endpoint testing with database and ESI data joins
*   [ ] **Test Coverage:** >85% coverage for all new functionality
*   [ ] **Test Data Management:** Proper test data setup for contracts, items, and ESI type cache
*   **Notes:** Testing strategy follows the pragmatic hybrid approach with integration tests for the API and unit tests for business logic.

### 7.4. Accessibility (A11y)
*   [x] **Semantic HTML/Structure:** N/A - Backend API task
*   [x] **ARIA Attributes:** N/A - Backend API task
*   [x] **Keyboard Navigability:** N/A - Backend API task
*   [x] **Color Contrast:** N/A - Backend API task
*   [x] **Screen Reader Compatibility:** N/A - Backend API task
*   [x] **Alternative Text for Images:** N/A - Backend API task (image URLs provided for frontend)
*   **Notes:** This is a backend API task that provides data for frontend consumption. The frontend will handle accessibility concerns for the detailed contract view.

### 7.5. Internationalization (I18n)
*   [x] **Text Abstraction:** N/A - Backend API returns raw data
*   [x] **Locale-Specific Formatting:** N/A - Backend returns standard formats (ISO dates, numeric values)
*   [x] **UI Layout Adaptability:** N/A - Backend API task
*   [x] **Character Encoding:** UTF-8 encoding used throughout
*   **Notes:** This is a backend API that returns data in standard formats. Internationalization is handled by the frontend consuming this API.

---
