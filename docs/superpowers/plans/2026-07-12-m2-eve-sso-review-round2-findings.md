# M2 EVE SSO plan — Round 2 review findings (independent cold-read, 4 reviewers)

Source: plan-review-cycle round 2, four independent Fable cold-read reviewers
(phases 0-3, 4-6, 7-9, cross-cutting) against the plan + spec + pitfalls, 2026-07-12.
Severity: blocking / substantive / minor. **STATUS: ALL 43 APPLIED** in round 3 of the
plan-review-cycle (runner application pass, 2026-07-12), together with the three
design deltas from the plan's `### Discoveries`. Kept as the round-2 record;
duplicated findings (same issue surfaced by multiple reviewers) were applied once.

```
================== p03 — 9 findings
[blocking] Phase 1 / Task 1.1 — test_sso_fields_have_safe_defaults in src/fastapi_app/tests/core/test_config.py
  PROB: The test asserts SSO defaults on the ambient process singleton (get_settings()), which is built from the real app/backend/src/.env plus OS env vars. It asserts ESI_CLIENT_ID == "" and TOKEN_CIPHER_KEYS in ("", <hard-coded CI throwaway key>). AC-4's explicit next-day workflow is Sam filling .env with real ESI_CLIENT_ID/ESI_CLIENT_SECRET/TOKEN_CIPHER_KEYS — from that moment `pdm run pytest` fails locally on this test for every later phase's Standing-Orders verification run. It also hard-couples the test to the exact throwaway key literal in ci.yml, whose own comment says "Regenerate any time" — 
  FIX : Assert class defaults on an isolated instance, not the singleton: monkeypatch.delenv every SSO/session env var (raising=False) and construct Settings(_env_file=None, DATABASE_URL="postgresql+asyncpg://u:p@localhost/x", CACHE_URL="redis://localhost:6379/9", ESI_USER_AGENT="test"), then assert TOKEN_CIPHER_KEYS.get_secret_value() == "" exactly (drop the CI-key tuple). Keep only the singleton-identity test (get_settings() is settings) against the ambient instance.

[substantive] Phase 1 / Task 1.3 Step 4 — "No import references fastapi_app.config anymore — verify with grep -rn \"fastapi_app.config\\b\\|from .config import\\|from ..config import\" src"
  PROB: This verification can never come back clean: after the migration, core/logging.py, core/esi_client_class.py, and core/dependencies.py legitimately contain `from .config import ...` (resolving to core.config), and cache.py/http_client.py post-edit also match the `from .config import` alternative. A fresh subagent told "no import references ... anymore" sees 5+ hits from the plan's own prescribed end state and either loops on an unsatisfiable gate (risking the 3-attempt escalation path) or "fixes" correct core-relative imports (e.g. rewriting core/cache.py to `from .core.config import`, an Impor
  FIX : Replace the Step 4 grep with `grep -rn "fastapi_app\.config" src` (expected: zero hits) plus `test ! -f src/fastapi_app/config.py`, and add a note that `from .config import` inside src/fastapi_app/core/ resolves to core.config and is expected to remain.

[substantive] Phase 0 / Task 0.1 Steps 2–3 and Task 0.2 Steps 1–4
  PROB: The step ordering is self-contradictory for a literal executor: Task 0.1 Step 2 says "push and observe" before any commit exists (Step 3 is the commit), and simultaneously says "Do Task 0.2 first; the two jobs share one file." If the executor appends the frontend job first as instructed, Task 0.1 Step 3's `git add .github/workflows/ci.yml` + commit "ci: add GitHub Actions backend job" stages BOTH jobs under a backend-only message, and Task 0.2 Step 4's commit then fails with "nothing to commit." Since this repo merges with --merge and treats every commit subject as a permanent record (CLAUDE.m
  FIX : Restructure: Task 0.1 = write backend job + commit it; Task 0.2 = append frontend job + local verification + commit it; then a single phase-level step: push the branch, open the PR, watch CI with a monitor, fix ci.yml (further `ci:` commits) until both jobs are green.

[minor] Phases 0–3 (verified absent plan-wide via grep) — spec §4.4 "One startup warning is logged in development" when SSO is not configured
  PROB: The spec requires a single development-mode startup warning when ESI_CLIENT_ID / TOKEN_CIPHER_KEYS are unset (§4.4). No task anywhere in the plan implements or tests it — `grep -in "warn"` over the plan matches only the Standing Orders' pristine-output rule. A cold executor of Phases 1/3/7 has no instruction to add it, so the spec item silently drops.
  FIX : Add the warning to the phase that wires main.py (Phase 7's lifespan/startup edit is the natural home, since Phase 1 removes prints and Phase 3 owns the cipher's is_token_cipher_configured() predicate it should reuse), with a caplog test asserting it logs exactly once in development and not in production.

[minor] Phase 2 / Task 2.3 — test_create_db_tables_gate.py
  PROB: The task's Gates line cites testing-pitfalls §6 Boundary — "environment-specific behavior tested both ways" — but only the production-skip direction is specified. Nothing asserts that ENVIRONMENT == "development" still reaches engine.begin()/drop_all/create_all, so a regression that makes create_db_tables() an unconditional no-op (while still logging "Skipping") passes the written suite and silently kills dev table creation (ENV-2's expected behavior).
  FIX : Add the mirror test: monkeypatch ENVIRONMENT to "development", stub main_mod.async_engine.begin with a recording async context manager (run_sync recorded, no real DB), call create_db_tables(), and assert begin() was entered and drop_all/create_all were passed to run_sync.

[minor] Phase 3 / Task 3.3 — src/fastapi_app/tests/core/test_session.py
  PROB: get_optional_session ships with zero coverage: no test for the anonymous path (no cookie → None) or the authenticated path (valid cookie → payload). The Standing Orders' pre-completion gate requires coverage for everything the task added; a signature or cookie-name regression in get_optional_session would be invisible until Phase 6.
  FIX : Add two tests mirroring the get_current_session ones: SimpleNamespace request with empty cookies → returns None (no exception); request with {settings.SESSION_COOKIE_NAME: sid} for a created session → returns the payload.

[minor] Phase 3 / Task 3.3 — test_get_current_session_503_when_cache_absent
  PROB: The test calls the pre-existing core.dependencies.get_cache directly and never touches session code, so it passes even if get_current_session drops its Depends(get_cache) seam entirely — it re-tests existing infrastructure and gives false confidence that the session dependency 503s when Valkey is absent (spec §4.2 contract). This is the "testing the mock/seam instead of the behavior" shape testing-pitfalls warns about.
  FIX : Either assert the wiring itself (e.g. inspect.signature(sess.get_current_session) shows the redis parameter's default is Depends(get_cache)) alongside the get_cache 503 behavior, or drop the test here and note explicitly that the HTTP-level 503-when-cache-absent test lands in Phase 6 (the spec §6 auth-fixture test), so the coverage gap is a tracked forward dependency rather than an accidental one.

[minor] Phase 2 / Task 2.1 — test_user_defaults_instantiate, line "assert isinstance(datetime, type)  # sanity; server defaults fill created_at at flush"
  PROB: The assertion is vacuous — isinstance(datetime, type) is always True and verifies nothing about created_at/updated_at server defaults. It's dead weight that looks like coverage.
  FIX : Delete the line (the comment about flush-time server defaults can stay), or replace it with a real check via the db_session fixture: add+flush a User and assert created_at/updated_at are populated datetimes.

[minor] Phase 1 / Task 1.4 Step 2 and Phase 2 / Task 2.3 Step 5 — commit blocks' `git rm --cached src/fastapi_app/config.py` / `git rm --cached src/fastapi_app/models/common_models.py`
  PROB: Earlier steps (Task 1.3 Step 2, Task 2.2 Step 4) already ran `git rm`, which deletes the file AND stages the deletion; the follow-up `git rm --cached <path>` then fails with `fatal: pathspec ... did not match any files`, a confusing error mid-commit-sequence for a literal executor despite the hedging comment.
  FIX : Drop both `git rm --cached` lines (the deletions are already staged), or replace with a no-op-safe check like `git status --short | grep config.py` to confirm the staged deletion.

================== p46 — 12 findings
[blocking] Phase 6 / Task 6.4, test_session_cookie_secure_flag_follows_environment (plan line ~2223): `assert "HttpOnly" in set_cookie and "SameSite=lax" in set_cookie.lower().replace("samesite=lax", "samesite=lax")`
  PROB: The SameSite conjunct is always False: `set_cookie.lower()` lowercases the whole header, then the code searches it for the mixed-case string "SameSite=lax" (the .replace() is a no-op). Verified against the repo venv: Starlette emits `SameSite=lax` and the expression evaluates False, so this parametrized test fails on every run. A fresh subagent hits a red test whose assertion is self-contradictory and — per the plan's own TEST-2 warning about brittle SameSite casing in Step 2 — is at high risk of 'fixing' it by weakening or deleting the SameSite check on the security-critical session cookie.
  FIX : Replace the conjunct with `assert "httponly" in set_cookie.lower() and "samesite=lax" in set_cookie.lower()`. While touching it, add the spec §6 cookie-attribute assertions the plan dropped: `"path=/" in set_cookie.lower()` and `f"max-age={settings.SESSION_ABSOLUTE_TTL_SECONDS}"` for hb_session (spec §6: 'assert the callback Set-Cookie for hb_session carries HttpOnly, SameSite=Lax, Path=/, Max-Age').

[substantive] Phase 6 / Task 6.4, test_state_is_single_use (plan lines ~2199-2208), first callback: `cookies={"hb_sso_state": "ONCE"}` with state="ONCE" and comment '(binding fails but GETDEL still consumed)'
  PROB: The comment contradicts the code: the cookie value equals the state, so binding SUCCEEDS and the handler proceeds to exit (D) — a code exchange on the fixture's real httpx.AsyncClient with NO httpx_mock in the test signature. That is a live network POST to https://login.eveonline.com/v2/oauth/token from a unit test. Worse, it passes silently either way: with network, EVE returns 400 → SsoTokenError → sso=error redirect; offline, ConnectError is caught by _post_token → same redirect — so the state key is consumed and both assertions hold while the test does non-hermetic live I/O (violates the r
  FIX : Make the first callback take the binding-failure path as the comment intends: pass a mismatched cookie (`cookies={"hb_sso_state": "OTHER"}`) and assert the 400, then assert `exists("sso_state:ONCE") == 0` (GETDEL consumed even on the 400 exit). Alternatively keep the matching cookie but add httpx_mock with a registered token response. Either way, no test using auth_client may reach exit (D) without httpx_mock.

[substantive] Phase 6 / Task 6.4, all flow tests passing per-request cookies (e.g. plan lines ~2109, 2121, 2129, 2137, 2146, 2156, 2166, 2177, 2190, 2204, 2220, 2239, 2254): `auth_client.get(..., cookies={...})`
  PROB: Verified against the repo venv (httpx 0.28.1): passing `cookies=` per request on a client instance emits `DeprecationWarning: Setting per-request cookies=<...> is being deprecated...` on every call. Roughly a dozen Phase 6 tests do this, so the suite's warnings summary is polluted — directly violating the Standing Orders' pristine-test-output gate (testing-pitfalls §1) — and the parameter is on a removal path in httpx. A fresh subagent following the plan verbatim ships a noisy suite and may burn cycles diagnosing the warnings.
  FIX : Set cookies on the client jar before the request (`auth_client.cookies.set("hb_sso_state", "STATE123")` / `auth_client.cookies.set(settings.SESSION_COOKIE_NAME, sid)`) — the fixture client is function-scoped so jar persistence is harmless — or send an explicit header (`headers={"cookie": f"hb_sso_state={state}"}`). Add one line to Task 6.4's Gates noting httpx 0.28 deprecates per-request cookies.

[substantive] Phase 4 / Task 4.2 (JWT quirk tests, plan lines ~1259-1361) and Phase 6 _inject_jwks (plan lines ~2061-2064)
  PROB: Spec §6 explicitly lists 'kid-miss behavior' among the JWT quirk tests, and spec §3.2/§6 claims 'the validator still runs for real (signature, kid selection, alg allowlist)'. The plan's _StaticKeyProvider 'returns one signing key regardless of kid lookup' and Phase 6 injects a SimpleNamespace the same way, so kid selection is never exercised anywhere in M2 — the production PyJWKClient kid-matching path (including PyJWKClientError on unknown kid) ships with zero coverage, and the spec-listed kid-miss test is silently dropped. The spec's stronger sanctioned option (real PyJWKClient with `PyJWKCl
  FIX : Add to Task 4.2: a test that builds a real `jwt.PyJWKClient` with `fetch_data` monkeypatched to return a JWKS dict containing the test RSA key under kid 'JWT-Signature-Key', asserting (a) a token with that kid validates (kid selection ran for real) and (b) a token signed with an unknown kid raises (PyJWKClientError / mapped SsoJwtError) — and confirm validate_access_token's except clause covers the kid-miss exception type PyJWKClient raises (jwt.exceptions.PyJWKClientError is NOT a subclass of I

[substantive] Phase 5 / Task 5.3, refresh_user_tokens (plan lines ~1684-1704): `except sso.SsoTokenError: await mark_for_reauth(db, user); return`
  PROB: SsoTokenError is raised by _post_token for ANY failure — transport errors (httpx.HTTPError) and every non-200 status alike. Spec §4.3 scopes mark-for-reauth to the 'Invalid-grant response' specifically. As written, a transient network blip or an EVE 5xx during refresh permanently nulls the user's encrypted refresh token, forcing re-login for an outage that would have healed on retry. The tests reinforce the conflation: only the 400 invalid_grant path is covered; there is no test that a 500/transport failure preserves the token columns.
  FIX : Have SsoTokenError carry the response status (and optionally the error code from the JSON body), set in _post_token (Phase 4). In refresh_user_tokens, null the esi_* columns only when the failure is an invalid-grant-shaped 400; re-raise (or return without mutating) on transport errors and 5xx. Add tests: 400 invalid_grant → columns nulled (existing); 500 and transport error → columns unchanged and the failure surfaced to the caller.

[substantive] Phase 6 / Task 6.2, _signing_key_provider (plan lines ~1863-1865) called per-request in the callback (line ~1921)
  PROB: The callback constructs a NEW `jwt.PyJWKClient(...)` on every invocation, so its internal JWKS cache (the 300 s TTL + kid-keyed LRU the spec's §3.2 choice rationale and Appendix A 'fires on cache miss only' argument depend on) is always empty — every login performs a fresh synchronous urllib JWKS fetch. Functionally tolerable at login frequency, but it contradicts the spec's stated design and negates the caching that justified choosing PyJWKClient over joserfc.
  FIX : Make the provider a lazily-created module-level singleton — e.g. decorate `_signing_key_provider` with `@functools.lru_cache(maxsize=1)` (or an explicit module global built on first call) so the PyJWKClient and its JWKS cache persist across requests. The monkeypatch seam survives: tests monkeypatch the module attribute `auth_api._signing_key_provider`, which shadows the cached callable. Note in the task that the singleton must be keyed off/rebuilt if ESI_SSO_JWKS_URI changes only in tests that m

[minor] Phase 4 / Task 4.1 test_build_authorize_url_has_exact_params (plan line ~1193) and Phase 6 / Task 6.4 login test (line ~2081): `assert q.get("scope", [""]) == [""]`
  PROB: Verified: `parse_qs` drops blank values by default, so `q.get("scope")` is None whether or not `scope=` is present, and the default `[""]` makes the assertion pass vacuously. The empty-scope requirement (F004: no ESI scopes) is asserted by a check that cannot fail — if a subagent drops the scope param from build_authorize_url entirely, both tests stay green.
  FIX : Parse with `parse_qs(split.query, keep_blank_values=True)` in both tests and assert `q["scope"] == [""]` (KeyError if scope is missing, mismatch if non-empty).

[minor] Phase 6 / Task 6.2 callback, exit (A) EVE-side denial (plan lines ~1886-1891) ordered before the binding check (C)
  PROB: Spec §4.1 step 4 frames the state-binding check as the callback's first gate ('Require both that the cookie value equals the returned state ... and that the GETDEL hit'), with the denial exit listed after the binding-fail 400. The plan's code short-circuits on `error` before validating the binding cookie, so `error=access_denied` + live state + mismatched/missing cookie yields a friendly sso=denied redirect instead of the spec's hard 400. Security impact is nil (no session is minted on the denial path), but the ordering is an unpinned deviation — no test covers error-param-plus-binding-mismatc
  FIX : Either reorder so the binding check precedes the denial exit (matching the spec's gate framing) and add a test for error+cookie-mismatch → 400, or add one sentence to Task 6.2 explicitly sanctioning denial-before-binding (rationale: no session is created, innocent-user UX wins) plus a test pinning the chosen behavior.

[minor] Phase 6 (goal + tasks) and Phase 7 — spec §4.4 final sentences: 'One startup warning is logged in development' when SSO is unconfigured
  PROB: The spec requires a single development-mode startup warning when ESI_CLIENT_ID / TOKEN_CIPHER_KEYS are unconfigured. No task in Phase 6 (guard/routes) or Phase 7 (main.py wiring) implements or tests it — the requirement is silently dropped from the plan.
  FIX : Add a step to Phase 7's main.py task (the phase that already edits lifespan-adjacent code): in lifespan startup, when ENVIRONMENT == 'development' and (not settings.ESI_CLIENT_ID or not is_token_cipher_configured()), log one warning ('EVE SSO not configured; /auth/sso/* will return 503'). Add a caplog test alongside the create_db_tables gate pattern.

[minor] Phase 5 / Task 5.1 (plan lines ~1501-1517) — 'Gates: Standard TDD gates' but the task contains only a create-file step, no test
  PROB: The Standing Orders make TDD (failing test first) mandatory for every task, but Task 5.1 provides no test step for CurrentUserSchema. A fresh subagent must either violate the gate or invent a schema test the plan didn't specify — interpretation latitude on a task that was meant to be trivial.
  FIX : Add one sentence to Task 5.1: 'This schema's behavior is exercised by Phase 6's /me flow tests and Phase 7's app.openapi() shape test; no standalone unit test is required — creating the file with the exact shape above satisfies this task.' (Or add a two-line shape test if strict TDD form is preferred.)

[minor] Phase 6 / Task 6.4, test_logout_is_idempotent_204_and_expires_cookie (plan line ~2233): `assert "hb_session=" in resp.headers.get("set-cookie", "")`
  PROB: Spec §6 requires asserting 'the logout response's expiring Set-Cookie'. The substring check passes for ANY hb_session Set-Cookie — including one that (re)sets a live value — so it does not verify the cookie is actually being expired.
  FIX : Assert the expiry mechanism: `sc = resp.headers["set-cookie"].lower()` then `assert "hb_session=" in sc and ("max-age=0" in sc or "expires=" in sc)` (Starlette delete_cookie emits Max-Age=0).

[minor] Phase 6 / Task 6.4, test_callback_happy_path_creates_user_sets_session_cookie (plan lines ~2113-2114)
  PROB: Spec §6's callback happy-path test requires 'tokens encrypted — assert ciphertext ≠ plaintext and round-trips'. The plan asserts non-None and ≠ plaintext but never decrypts, so the flow-level test can't distinguish real Fernet ciphertext from any other transformation (the round-trip exists only at the Phase 5 service layer, where the cipher key is monkeypatched separately).
  FIX : Add to the happy-path test: `from fastapi_app.core import token_cipher as tc` and assert `tc.decrypt_token(user.esi_access_token)` equals the signed access token and `tc.decrypt_token(user.esi_refresh_token) == "RT"` (the configured_sso fixture's key is still active, so decryption uses the same keyring the request used).

================== p79 — 11 findings
[blocking] Phase 8 / Task 8.6 / Step 3 — "In `detail.spec.ts`, place it before `failUnexpectedApiCalls(page)` is registered so the 401 handler wins (last-registered-first, but `interceptCurrentUser` uses a specific regex that still matches)"
  PROB: The instruction is inverted and its rationale is self-contradictory. Playwright consults route handlers last-registered-first with NO specificity ranking; `failUnexpectedApiCalls` (e2e/helpers/api.ts:96-107) registers a `**/api/v1/**` catch-all that calls `route.abort('failed')` and never falls back, and its own docstring says "Register FIRST … so specific intercepts still win." If a fresh subagent registers `interceptCurrentUser` BEFORE `failUnexpectedApiCalls` (as instructed), the later-registered catch-all handles `/me` first and aborts it in the two detail.spec.ts tests that use it (lines 
  FIX : Rewrite Step 3's detail.spec.ts note: in the tests that call `failUnexpectedApiCalls(page)` (detail.spec.ts:98, 132), register `interceptCurrentUser(page, { status: 401 })` AFTER `failUnexpectedApiCalls`, matching the helper's documented register-catch-all-first pattern. Delete the false "specific regex still matches" parenthetical.

[substantive] Phase 8 / Task 8.4 / Steps 1–2 ("In `pages.test.tsx` and `a11y.test.tsx`, wrap every `stubFetch(...)` handler with `anonymousMe(...)`") and the Phase 8 Files list
  PROB: The existing-component-test sweep omits `src/routes.test.tsx`, which also mounts the full routeTree via `renderApp` with its own URL-agnostic `stubFetch` (routes.test.tsx:6-15). Its first test (line 20) answers every URL — including the new `GET /api/v1/me` — with a contract-shaped 200 (`{total:0,page:1,size:50,items:[]}`), producing exactly the bogus authenticated header (undefined name, `.../characters/undefined/portrait` src) the spec's §6 sweep rule targets. The test would still pass, silently embedding the non-hermetic state. (`hooks.test.tsx` uses `renderHook` only — correctly out of sco
  FIX : Add `src/routes.test.tsx` to the sweep: import `anonymousMe` and wrap both of its `stubFetch(...)` handlers, add the file to the Phase 8 Files list and to the Task 8.6 Step 6 `git add` list.

[substantive] Phase 8 / Task 8.5 / Step 1 — "Inspect `src/features/contracts/components/FilterRail.tsx` price fieldset (lines ~71–110) against the sibling numeric range fieldsets (collateral/runs)"
  PROB: The comparison target does not exist. FilterRail.tsx has exactly three fieldsets — Show (legend line 58), Price (ISK) (line 72), Regions (line 113); there are no collateral or runs fieldsets anywhere in the file (grep for collateral/runs returns nothing). A fresh subagent hunting for the named siblings finds nothing, and the locate-or-N/A branch collapses to "no divergence found → mark N/A" without ever inspecting the actual drift. The real 14px-vs-13px candidates are: the shared `Input` component's base `text-sm` (14px, Input.tsx:6) fighting the appended `text-data` override (13px via `--text
  FIX : Replace the false reference with the real comparison: "the price Min/Max `Input`s carry both the component-default `text-sm` (14px) and the appended `text-data` (13px `--text-meta`); verify which wins under the Tailwind v4 @utility ordering and unify so the price inputs render at the intended 13px data size, matching the M1 handoff nit. If the `text-data` override already reliably wins, record N/A in Discoveries." Also add FilterRail.tsx to the Phase 8 Files list (it appears only in the Step 6 c

[substantive] Phase 8 / Task 8.6 / Step 2 `interceptLogout` + Step 4 logout spec ("logout POSTs exactly once and returns to anonymous")
  PROB: Spec §6 requires the logout spec to assert "exactly one POST to `/api/v1/auth/sso/logout` fulfilled 204." The planned `interceptLogout` captures only `{url, params}` (reusing `CapturedCall`) and `page.route` matches ALL methods, and the spec file asserts only `logoutCalls.length === 1` — the request METHOD is never verified at the E2E layer. A regression that fired the logout as a GET (or any other method) would still pass this test.
  FIX : Capture the method in `interceptLogout` (e.g. push `{url, params, method: route.request().method()}` with a local type extending `CapturedCall`) and add `expect(logoutCalls[0].method).toBe('POST')` to the logout spec, mirroring the vitest `useLogout` test which does assert the method.

[substantive] Phase 8 / Task 8.6 / Step 4 — logout spec: `await page.getByRole('button', { name: /log out/i }).click()` followed by `authed = false`
  PROB: The `authed = false` flip happens in Node only after `click()` resolves. The in-page mutation (POST logout → 204 → onSettled invalidate → `/me` refetch) runs asynchronously; if the logout route fulfillment and the `/me` refetch both complete while `click()` is still awaiting actionability/dispatch confirmation, the refetch is served with `authed` still true, the header stays authenticated, and the final anonymous-link assertion fails. With Playwright `retries: 0` and the repo's hard anti-flake rule (TEST-2: fix with deterministic synchronization, never weaken), this ordering is a latent flake 
  FIX : Make the flip deterministic: set `authed = false` inside the logout route handler (before/at `route.fulfill({status: 204})`) so the `/me` refetch — which can only fire after the 204 — always observes the anonymous state. Remove the post-click flip.

[substantive] Phase 7 / Task 7.2 — no step touches the Phase 6 `auth_client` fixture (plan lines 1986-2006: `real_app.include_router(auth_api.router)` … `del real_app.router.routes[n_routes:]   # restore routes — main.py stays unmounted (D2)`)
  PROB: Once Phase 7 mounts both auth routers in `main.py`, the Phase 6 `auth_client` fixture still `include_router`s them a second time on every auth test — duplicating all four route paths for the duration of each test — and its load-bearing comment "main.py stays unmounted (D2)" becomes actively false (violating the comments rule: comments must not be provably wrong). Requests still resolve (first match wins) so the suite likely stays green, but any future test that generates `app.openapi()` while the fixture is live gets duplicate-operation-ID warnings (breaking the pristine-output gate), and a fr
  FIX : Add a step to Task 7.2: after mounting the routers in `main.py`, strip the now-redundant fixture machinery from `conftest.py`'s `auth_client` — the two `include_router` calls, the `n_routes` snapshot/truncate, and both `openapi_schema = None` resets — and delete the stale D2 comment. Re-run the full suite to confirm the fixture still works against the production-mounted routes.

[substantive] Phase 9 / Tasks 9.1–9.2 — Files list: "Modify `design/features/F004...`, `README.md`, `docs/pitfalls/implementation-pitfalls.md`"
  PROB: The docs sweep leaves three documents actively false after M2 lands. (1) implementation-pitfalls.md ENV-1's "Also note" says "there are two divergent Settings classes (`fastapi_app/config.py` and `fastapi_app/core/config.py`) — setup docs must satisfy both", and the §3.C checklist (line 145) says "both `config.py` and `core/config.py` Settings classes are satisfied (ENV-1)" — false once Phase 1 deletes `config.py`. The doc's own Appendix C framework mandates updating an entry when its prescribed fix is implemented. (2) CLAUDE.md's "Two Settings classes trap" architecture bullet ("There are cur
  FIX : Add a Phase 9 step: rewrite ENV-1's "Also note" and the §3.C checklist line to describe the single consolidated `core/config.py` Settings (keeping the JSON-decode trap itself, which remains live), add an Appendix A changelog line, and update the CLAUDE.md + AGENTS.md "Two Settings classes trap" bullet in the same PR (sibling-sync). Add all three files to the Phase 9 Files list.

[minor] Phase 8 / Task 8.3 / Step 2 — HeaderIdentity.test.tsx: `const img = screen.getByRole('presentation', { hidden: true }) as HTMLImageElement | null` and the `waitFor` import
  PROB: The `img` variable is dead code (the real assertion uses `document.querySelector` on the next line) and `waitFor` is imported but never used in this file — both trip `npx eslint .` no-unused-vars, failing the phase verification gate. Worse, `getByRole('presentation')` throws if it doesn't match (the `| null` cast is meaningless), and querying the `presentation` role for an `alt=""` image is unreliable across testing-library/dom-accessibility-api versions — the test can fail for a reason unrelated to the component, muddying the TDD confirm-it-fails-for-the-stated-reason step.
  FIX : Delete the `const img = ...` line and the unused `waitFor` import from the verbatim test; keep the `document.querySelector('img[src*="images.evetech.net"]')` assertion, which fully covers the decorative-portrait check.

[minor] Phase 8 / Task 8.4 / Step 1 — "Add a shared URL-aware helper to `src/test/http.ts`" with a code block that includes `jsonResponse`
  PROB: `jsonResponse` already exists verbatim in `src/test/http.ts` (lines 1-6). The instruction says "Add" but the block re-includes the existing function; a literal-minded subagent could append the whole block and produce a duplicate declaration (compile error) or wonder whether the existing function is meant to change.
  FIX : Reword to: "src/test/http.ts already exports `jsonResponse`; append only the `anonymousMe` wrapper below it. The block shows the file's final state."

[minor] Phase 8 / Task 8.1 / Step 4 — useCurrentUser queryFn: `const { data } = await api.GET('/me'); return data ?? null`
  PROB: Spec §5 says "401 (or any failure) resolves to `null` (anonymous) rather than throwing." openapi-fetch returns `{data: undefined, error}` for HTTP error statuses (so 401/500 resolve null as intended) but THROWS on network-level failures, so those reject, hit the global retry:1, and land in error state instead of resolving null. The rendered outcome is still anonymous (HeaderIdentity's `!user` branch), so this is a narrowing rather than a user-visible bug — but it silently diverges from the spec's stated contract and a reviewer comparing hook to spec will trip on it.
  FIX : Either wrap the queryFn body in try/catch returning null (implementing "any failure"), or add a one-line comment + plan note recording the deliberate narrowing: network errors reject (retry:1 applies) while HTTP failures resolve null; the header renders anonymous in both cases.

[minor] Phase 9 / Task 9.2 / Step 3 — "Add any coordinator-supplied discovered traps the same way"
  PROB: "The same way" binds all coordinator-supplied traps to `implementation-pitfalls.md`, but M2's likeliest discovered traps are testing traps (e.g. the every-spec-must-intercept-`/me` rule, the ASGITransport-no-lifespan app.state fixture pattern), which belong in `docs/pitfalls/testing-pitfalls.md` — a file absent from Phase 9's Files list and with its own different add-a-pitfall framework (checklist-item format, TEST-N numbering).
  FIX : Amend Step 3: route each supplied trap to the correct file — implementation traps to implementation-pitfalls.md per its Appendix C framework, testing traps to testing-pitfalls.md §8 per its own "How to Add a Testing-Pitfall" section — and add testing-pitfalls.md to the Phase 9 Files list as a conditional target.

================== cross — 11 findings
[substantive] Phase → PR mapping — "Phases 0, 1, 2, 5, 7, 8, 9 are `Routine`"
  PROB: Merge classifications are under-inclusive against the repo merge-authority domain triggers (CLAUDE.md / git-strategy: 'auth, secrets, crypto; data-integrity paths; architecture changes like public interfaces, serialization contracts, schema, external APIs'). Phase 5 IS the encrypted token vault write path + owner-hash transfer rule (auth/secrets/data-integrity) yet is classified Routine; Phase 2 changes the DB schema (new User + token-vault columns); Phase 7 changes the public API surface and the serialization contract (OpenAPI/schema.d.ts). Under the spec's original 3-PR grouping all of this 
  FIX : Reclassify Phase 5 as `Review — domain (security: token vault/auth service)`. For Phases 2 and 7, either reclassify as `Review — domain (schema / public interface)` or add one sentence in the Phase → PR mapping explicitly justifying why Routine is claimed despite the schema/public-interface triggers, so the executor isn't making that call implicitly.

[substantive] Phase 6 Task 6.2 (api/auth.py code) + Phase 4 Task 4.2 (sso.py code) — no logging anywhere
  PROB: Spec §4.1 step 5 requires 'structured log + redirect' on token-endpoint non-200, step 6 requires 'log + redirect' on JWT failure, and §7 pins the shape: 'log_key_event payloads carry character_id/outcome/duration only'. The house helper exists (app/backend/src/fastapi_app/core/logging.py:113, used by contract_service.py). The plan's api/auth.py and sso.py listings contain zero logger/log_key_event calls, so a fresh subagent implementing verbatim ships the security-observability decision the spec explicitly encoded... deleted.
  FIX : In Task 6.2, add `log_key_event` (or structlog) calls on the token-exchange-failure and JWT-failure callback exits (and optionally login/denial outcomes), carrying character_id/outcome/duration only — never token material (§7). Add a Gates bullet: 'no tokens/JWTs/cipher keys in log payloads'. Capture the expected log lines in the Task 6.4 tests (caplog) so output stays pristine.

[substantive] Phase 4 Task 4.2 JWT quirk tests + Phase 6 Task 6.4 flow tests
  PROB: Spec §6 backend list explicitly requires a 'kid-miss behavior' quirk test. No plan test exercises it: `_StaticKeyProvider` ignores kid entirely, and Phase 6's `except (sso.SsoJwtError, KeyError, jwt.PyJWKClientError)` branch — the only place a kid miss surfaces — is never driven by any test. An executor could break kid-miss handling (e.g. let PyJWKClientError escape as a 500) with the suite green.
  FIX : Add a Phase 4 test where the injected key provider raises `jwt.PyJWKClientError` (unknown kid) and assert `validate_access_token`/caller behavior, plus a Phase 6 flow test where `_inject_jwks` raises PyJWKClientError and the callback redirects `?sso=error` (not 500). While there, cover the §7 'nbf validated when present' claim with one nbf-in-future-beyond-leeway rejection test.

[substantive] Phase 8 Task 8.4 Step 2 — "In `pages.test.tsx` and `a11y.test.tsx`, wrap every `stubFetch(...)`"
  PROB: The /me=401 sweep misses `app/frontend/web/src/routes.test.tsx`, which also mounts the real routeTree via `renderApp` with its own URL-agnostic local `stubFetch` (verified: routes.test.tsx lines 4–29, `stubFetch(() => jsonResponse({ total: 0, ... }))`). Once HeaderIdentity lands, its stubs answer `/api/v1/me` with a contract-shaped 200 → the exact bogus-authenticated-header state spec §6 warns about ('name undefined, portrait src .../characters/undefined/portrait'). The Files list and the Step 6 commit also omit it, so a fresh subagent has no reason to touch it.
  FIX : Add `src/routes.test.tsx` to the Phase 8 Files list, to Task 8.4 Step 2's sweep instruction (wrap its stubFetch handlers with `anonymousMe(...)`), and to the Step 6 `git add` list.

[substantive] Phase 6 Task 6.4 test code — every request passing `cookies={...}` per-request (≈12 tests, e.g. test_callback_happy_path..., test_logout_deletes_existing_session, test_me_200...)
  PROB: The pinned httpx 0.28.1 emits a DeprecationWarning for per-request `cookies=` (verified in the repo venv: httpx/_client.py:806 and :1521 — 'Setting per-request cookies=<...> is being deprecated'). The plan's Standing Orders require pristine test output ('no stray errors/warnings'), so a fresh subagent implementing the provided test code verbatim hits a wall of warnings across the whole flow suite with no guidance, risking either a filterwarnings suppression (masking) or ad-hoc rewrites.
  FIX : In Task 6.4, instruct: set cookies on the client (`auth_client.cookies.set("hb_sso_state", "STATE123")` before the request, clearing after) or send an explicit `headers={"Cookie": ...}` — do not pass per-request `cookies=` under httpx ≥0.28, and do not add a warnings filter.

[minor] Phase 3 Task 3.3 — session.py defines `get_optional_session`; no test anywhere covers it
  PROB: Spec §4 requires both `get_current_session` and `get_optional_session`; the plan ships the latter with zero tests and zero callers. Under the repo rule 'Tests MUST comprehensively cover ALL functionality', an executor either ships untested surface or improvises coverage.
  FIX : Add two small tests to test_session.py: no cookie → None; valid cookie → payload dict (and expired → None). Alternatively, if it should be dropped as YAGNI, that is a spec deviation requiring an explicit deviation note — say which.

[minor] Living Document Contract — "See Step 5's stale-claim reclaim protocol."
  PROB: No 'Step 5' or reclaim protocol exists anywhere in the plan; it's shorthand for the writing-plans-enhanced skill's internal step, which a fresh executor (told only to use subagent-driven-development or executing-plans) cannot resolve. This violates the repo's self-identifying cross-reference rule inside the very contract that binds executors.
  FIX : Inline the reclaim protocol in the contract (e.g. 'a 🚧 claim with no PR and no branch commits in N hours may be reclaimed: flip the banner back with a note naming the stale claim'), or delete the sentence.

[minor] Standing Orders — Shared-file rule list ("main.py, models/__init__.py, pages.test.tsx, e2e/helpers/api.ts")
  PROB: src/fastapi_app/tests/conftest.py is edited by two phases (Task 1.3 import swap; Task 6.3 fixture append) but is absent from the shared-file list, and neither task's body cross-references the other's edit as the rule requires.
  FIX : Add conftest.py to the shared-file list; add one line to Task 1.3 ('Phase 6 appends auth fixtures to this file') and Task 6.3 ('append only — Phase 1 already swapped the settings import; do not touch existing fixtures').

[minor] Phase 8 Task 8.4 Step 1 — "Add a shared URL-aware helper to src/test/http.ts" (snippet includes jsonResponse)
  PROB: src/test/http.ts already contains `jsonResponse` verbatim (verified). The snippet shows both functions under an 'add' instruction, so a literal executor appends a duplicate `jsonResponse` declaration (tsc error) or wonders whether to replace the file.
  FIX : Reword to: 'src/test/http.ts already exports jsonResponse — append only the anonymousMe helper below.'

[minor] Phase 6 Task 6.4 cookie tests vs spec §6 cookie-attribute list
  PROB: Spec §6 requires asserting the callback hb_session Set-Cookie carries 'HttpOnly, SameSite=Lax, Path=/, Max-Age'. The plan's tests assert HttpOnly/SameSite/Secure but never Path=/ or Max-Age (=SESSION_ABSOLUTE_TTL_SECONDS) on hb_session — the Max-Age=absolute-cap decision (§4.2) goes unguarded.
  FIX : Extend the happy-path or secure-flag test to also assert `Path=/` and `Max-Age=2592000` in the hb_session Set-Cookie.

[minor] Whole plan — spec §4.4 "One startup warning is logged in development" has no task
  PROB: No phase implements or tests the development-mode startup warning when SSO is unconfigured (empty ESI_CLIENT_ID / TOKEN_CIPHER_KEYS). The requirement appears only in the spec; a fresh executor of any phase will not produce it.
  FIX : Add a step to Phase 7 (lifespan wiring in main.py is already being edited) — log one warning at startup when ENVIRONMENT=development and require_sso_configured's condition holds — with a caplog test.

```
