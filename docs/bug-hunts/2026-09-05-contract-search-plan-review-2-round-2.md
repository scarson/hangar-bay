<!-- ABOUTME: Records independent cold findings on the contract-search remediation implementation plan. -->
<!-- ABOUTME: Identifies source-verified execution gaps across the six plan-review dimensions. -->

**Substantive findings as raised: 1.**

1. **Task 5.1 — "Remove it from nullable sorts and join triggers"; "Update the two deliberate conflicting test groups specified in the planning evidence." Dimension: Testing pitfalls (TEST-25), context gaps.** Removing the Name-sort join trigger also silently disconnects an existing regression from the behavior it protects. In `app/backend/src/fastapi_app/tests/api/test_contract_filters.py:2801`, `test_a_page_past_the_end_of_the_joined_path_loads_from_an_empty_id_list` requests only region, `sort_by=ship_name`, direction, page 5 and size 2 (lines 2822-2825). After the planned `_needs_item_join` change, that request takes `_fetch_page_simple`, so its unchanged green assertions no longer exercise `_fetch_page_joined` loading an empty `page_ids` list through `Contract.contract_id.in_(page_ids)` (`services/contract_service.py:616-618`). The plan and selected SQL companion identify other sort-expectation updates but omit this test; the proposed joined-pagination cases cover valid pages and do not replace this distinct out-of-range branch. Explicitly retain the regression's joined path with a real join-triggering filter that matches both seeded contracts, such as title search `Paging`, preserve its empty-items/positive-total/requested-page assertions, and update the now-false join rationale in its docstring and `_paged_contract` helper. Otherwise execution reduces existing branch coverage while the full suite still passes.
