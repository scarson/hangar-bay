<!-- ABOUTME: Cross-provider (codex, read-only) adversarial pre-merge review of PR #46 (M3 account features). -->
<!-- ABOUTME: 7 findings (3 major, 4 minor), verdict DONE_WITH_CONCERNS; extracted 2026-07-18. All addressed in the post-review fix wave. -->

codex
1. **Major — [useSavedSearches.ts:12](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/features/saved-searches/hooks/useSavedSearches.ts:12), [useWatchlist.ts:13](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/features/watchlists/hooks/useWatchlist.ts:13), [useNotifications.ts:10](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/features/notifications/hooks/useNotifications.ts:10)** — Per-user query keys omit the authenticated identity. **Failure:** Tab A caches user A’s saved searches/watchlist/notifications; another tab replaces the shared session cookie by logging in as B. Tab A can resolve `/me` as B while retaining fresh A data under the unchanged keys, exposing A’s account data to B. **Fix:** include `character_id` in every account query key, enable queries only for that identity, and remove account caches on logout, 401, or identity transition.

2. **Major — [__root.tsx:9](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/routes/__root.tsx:9)** — Saved searches and watchlists have routes but no in-app navigation. **Failure:** a user can create a saved search or quick-watch a ship, but cannot reach either management page without knowing and manually entering its URL. The E2E tests conceal this by navigating directly to `/saved-searches` and `/watchlist`. **Fix:** add authenticated navigation links or a user menu covering both pages, and make E2E tests reach them through the UI.

3. **Major — [esi_client_class.py:230](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/backend/src/fastapi_app/core/esi_client_class.py:230), [watchlist_service.py:20](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/backend/src/fastapi_app/services/watchlist_service.py:20)** — Watchlist validation incompletely normalizes ESI failures. **Failure:** `ConnectTimeout`, `PoolTimeout`, protocol errors, or malformed 2xx JSON escape as 500s. ESI 420/429 throttling responses are classified as invalid user input and returned as 400 “unknown ship,” although ESI documents both as rate-limit responses. [Official ESI rate-limit documentation](https://developers.eveonline.com/docs/services/esi/rate-limiting/). **Fix:** retry/catch all `httpx.RequestError`, wrap decoding failures, classify 420/429 as retryable upstream failures, and add transport/rate-limit/malformed-response tests.

4. **Minor — [account.py:18](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/backend/src/fastapi_app/schemas/account.py:18), [account.py:64](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/backend/src/fastapi_app/schemas/account.py:64), [account.py:55](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/backend/src/fastapi_app/models/account.py:55)** — Price validation permits non-finite and storage-width-overflow values. Direct reproduction confirmed `1e309` becomes `inf`, while `1e20` passes watchlist validation despite exceeding `NUMERIC(20,2)`. **Failure:** valid-by-schema requests fail during JSON or numeric persistence and return 500 instead of 422. **Fix:** use constrained `Decimal` fields matching database precision/scale and explicitly reject non-finite values; test exact boundaries.

5. **Minor — [WatchlistPage.tsx:158](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/features/watchlists/components/WatchlistPage.tsx:158)** — Inline watchlist edits bypass the add form’s price validation and never display mutation errors. **Failure:** saving `0`, a negative value, or a non-finite number receives 422, but the row continues displaying the unsaved value with no indication that persistence failed. **Fix:** validate finite values ≥0.01 before mutation, render update failures, and reconcile local inputs with persisted data after success or failure.

6. **Minor — [WatchlistPage.tsx:99](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/features/watchlists/components/WatchlistPage.tsx:99), [WatchButton.tsx:13](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/features/watchlists/components/WatchButton.tsx:13), [client.ts:15](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/lib/api/client.ts:15)** — `ApiError` discards backend error details; the page labels every 400 as an unknown ship, while quick-watch silently ignores every failure except 409. **Failure:** at the 200-item cap, adding a valid ship reports a spelling problem; a quick-watch 400/502 appears to do nothing. **Fix:** retain a structured error code/detail and render cap, invalid-type, duplicate, and upstream-outage outcomes in both add paths.

7. **Minor — [NotificationsPage.tsx:40](/Users/sam/Code/hangar-bay/.claude/worktrees/m3-account-features/app/frontend/web/src/features/notifications/components/NotificationsPage.tsx:40)** — Retention pruning can strand notification pagination on a false empty state. **Failure:** pruning the sole item from page 2 returns `total: 20, items: []`; the UI says “No notifications yet” and removes pagination even though page 1 remains populated. **Fix:** clamp to the final valid page, matching `ContractsPage`, and reserve the global empty state for `total === 0`.

Sam, **overall verdict — DONE_WITH_CONCERNS:** the branch is not merge-ready because of the cross-user cache exposure, missing feature navigation, and ESI failure-classification defects. Backend ownership scoping, session-to-user verification, matcher deduplication/chunking/date gates/pruning, transaction handling, and OpenAPI/client parity survived review. The sandbox prevented refreshing Git metadata or reaching GitHub, so the reviewed baseline was the existing local `origin/dev` ref (`047b63130eeefff24d6d78622e529178ac2bd8f3`).
